torch==2.3.1
transformers>=4.41.0,<5.0.0
sentence-transformers>=3.0
torchaudio==2.3.1
torchvision==0.18.1
numpy==2.0.2
packaging<25.0

pytest>=7.0
python-dotenv>=1.0

# Optional manual-demo extras
# Install local embedder support with:
sentence-transformers>=3.0
# Install OpenAI embedder support with:
#   pip install openai
# Install both optional embedders with:
#   pip install openai sentence-transformers
# sentence-transformers>=3.0
# openai>=1.0

# Optional: for bonus tasks
chromadb>=0.4.0      # Real vector database integration
